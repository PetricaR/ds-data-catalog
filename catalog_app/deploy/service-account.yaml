apiVersion: v1
kind: ServiceAccount
metadata:
  name: ds-catalog-sa
  namespace: default
  annotations:
    # Workload Identity: allows this K8s SA to act as the GCP SA
    # Set by 2-setup-workload-identity.sh — do not edit manually
    iam.gke.io/gcp-service-account: ds-catalog-sa@formare-ai.iam.gserviceaccount.com
